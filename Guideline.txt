						ANACONDA PROMPT
				GPU Name: NVIDIA GeForce RTX 3060

Change directory to your project:
cd "C:\Users\Administrator\Desktop\Api_serverendpoint"

Create & activate a new Conda environment:
conda create -n ikioo_env python=3.10 -y
conda activate ikioo_env

Install Conda:
conda install pytorch torchvision torchaudio pytorch-cuda=12.4 -c pytorch -c nvidia -y

Install requirements.txt:
pip install -r requirements.txt

Install Torch:
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

Verify Python interpreter & torch (GPU) inside the conda env:
where python
python -c "import sys; print('PYTHON:', sys.executable)"
python -c "import torch; print('torch:', torch.__version__); print('torch.cuda:', torch.version.cuda); print('cuda available:', torch.cuda.is_available()); import torch; print('gpu name:', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'none')"
nvidia-smi

Run project:
python main.py

Check Installed Packages:
pip list

List Virtual EnvironmenmtsL
conda env list